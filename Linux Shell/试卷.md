下列描述如不做特别说明则都是正确描述

+   **Linux是一个免费的开源的操作系统。**

+   **Shell是一个命令解释器和~~编译型~~程序设计语言。**

    +   解释型语言,Shell脚本不需要经过编译

+   **为了能让其它用户使用`cd`命令进入该目录，需要设置其它用户具有~~读权限~~。**

    +   需要有执行(`-x`)权限

    >   +   Linux下文件的权限类型一般包括读read，写write，执行execute。对应字母为 `r、w、x`。
    >   +   目录的可执行权限是表示可否在目录下执行命令。如果目录没有`-x`权限，则无法对目录执行任何命令，甚至无法`cd `进入目录。即使目录仍然有`-r` 读权限（这个地方很容易犯错，认为有读权限就可以进入目录读取目录下的文件）。
    >   +   而如果目录具有`-x`(执行)权限，但没有`-r`(读)权限，则用户可以执行命令，可以`cd`进入目录。但由于没有目录的读权限，所以在目录下，即使可以执行`ls`命令，但仍然没有权限读出目录下的文档。

+   **使用`ls --long`命令可以以长列表的形式给出当前目录中的文件和子目录名。**

    +   >   错误,使用`ls -l`
        >
    
+   **即使拥有多块硬盘，Linux系统中所有的文件也都被放置到一个总的目录树中。**

+   **Shell 变量中~~只有~~字符串型变量，所以~~不能~~进行数值计算。**

    +   >    Shell默认变量是字符串类型,而不是只有字符串类型, 使用declare声明变量类型,可以进行数值运算

+   **Linux中的文件名~~不能~~以*、？、^等特殊符号来命名。**
    
    +   Linux中可以使用任何字符给文件命名,但是不建议使用特殊符号命名
    
+   **在字符界面环境下注销Linux shell，可用`exit`或ctrl+D.**
    
    +   正确,还可以使用`logout`
    
+   **如果需要中断一个进程可以使用Ctrl+C.**

+   **Linux中可以将一条命令作为一个判断条件，根据命令的执行成功与否赋值。**

+   **shell中切换用户的命令是~~set~~**

    +   `su`

    +   `set`功能是设置shell,在shell脚本中,使用`set -eu`可以:

        +   在扩展一个没有的设置的变量的时候,显示错误的信息  

        +    如果一个命令返回一个非0退出状态值(失败),就退出.

        +   >   如果没有使用,若程序异常（返回非 true）值， 程序会继续向下执行。

+   **支持颜色显示的终端，会以~~绿色~~显示目录。**
    +   黑(白)色表示普通文件
    +   蓝色:表示目录
    +   绿色:表示可执行文件
    +   红色:表示压缩文件
    +   浅蓝色:链接文件
    +   红色闪烁:表示链接的文件有问题
    +   黄色:表示设备文件
    +   灰色:表示其它文件

+   **`mv`指令可以移动一个文件的位置或者~~复制~~文件.**
    +   `mv`可以剪切,重命名文件

+   **`touch`命令可以改变一个文件的所有权。**
    +   错误,`touch`是创建文件
    +   `chmod`用于更改文件所有权

+   **输出重定向是指将标准输出的的内容发送到一个文件中，以保存其内容。**

+   **Linux中的变量不能以`*`、`?`、`^`等特殊符号来命名。**
    +   变量名必须是以字母或下划线字符“_”开头，后面跟字母、数字或下划线字符。不要使用?、*或其他特殊字符命名你的变量。

+   **`rm `可以删除任意的文件，`rmdir` 可以删除任意的目录。**
    +   错误, `rmdir`只能删除空目录 , 使用`rm`删除非空目录
+   **可以使用`paste`命令将剪贴板中的内容现实出来**
    +   错误 `paste`和`cut`与剪贴板没有关系
+   **Linux系统的最高管理权限属于root用户**

---

+   在Windows操作系统中可以使用的ssh远程登录工具有**putty**和**XShell**

+   Linux系统中用户的信息保存在`etc/passwd`文件中，加密后的密码保存在`etc/shadow`

+   为当前目录中的`execfile`文件增加可执行权限的命令语句是`chmod +x execfile` 
    +   `chmod` 可以更改文件权限,`+`表示加权限,`-`表示取消权限,`=`表示唯一设定权限
    +   `r=4`
    +   `w=2`
    +   `x=1`

+   将当前目录中的 work 目录及其中的所有子目录及文件进行打包的命令是`tar -cvf .` (压缩为tar包)
    +   解包: `tar -xvf`
    +   压缩为`xxx.tar.gz`使用 `tar -czvf`, 解包用 `tar - xzvf`

+   当输入一个命令回车以后，shell 解析出第一个词语作为命令，然后进行命令解释的顺序是`内置命令->关键字->函数->外部命令->别名`
+   列出磁盘分区信息的命令是`fdisk`,统计目录或文件所站磁盘空间大小的命令是`du`
    +   `du` = `disk usage`
        +   查看使用情况
    +   `df` = `disk free`
        +   查看剩余情况

+   Linux系统下表示标准输入、标准输出和标准错误输出的文件描述符分别为`0(STDIN)`,`1(STDOUT)`,`2(STDERR)`。

+   ls长型显示的某行第一列为：`drwxr-x--x`，`d`表示该项是一个`文件夹`,用数值形式表示该权限`751`,这表示文件的所有者具有`读,写,执行`的权限，而文件的所有组用户具有`读,执行`权限，其余用户的权限是`执行`
    +   `d`表示文件加,`-`表示普通文件,`l`表示软连接
    +   从第二个字母开始,每三个字母依次表示**拥有者**,**群组**,**其他组**权限
    +   `r=4`
    +   `w=2`
    +   `x=1`

+   `echo`命令的功能是<u>在显示器上显示一段文字，一般起到一个提示的作用</u>,`cat`命令的功能是<u>查看一个文件的内容</u>,并用重定向合并到一个文件中。后者常用来读取多个文件，

+   `pwd`命令可显示当前目录的绝对路径，而当前目录的符号表示是`./`，用户testuser的主目录的符号表示是`~/`
+   `vi`是linux下最常用的文本编辑器，是`sed`基于文本流的编辑器。`awk`是linux功能最强大的过滤器，

+   显示过去命令列表的命令是`history`，改变当前路径的命令是`cd`，显示当前日期和时间的命令是`date`，动态显示系统正在运行的程序的命令是`top`
    +   瞬间进程:`ps -aux`

---

+   用于终止某一进程执行的命令是`kill`
    +   `end`没有此命令
    +   `stop`没有此命令
    +   `free`:可以显示当前系统未使用的和已使用的内存数目，还可以显示被内核使用的内存缓冲区。

+   可以为文件或目录重命名的命令是`mv`
    +   `mkdir`:创建目录
    +   `rename`: 没有此命令
    +   `rm`:删除文件或目录(非空目录需要使用-rf)

+   显示一个文件最后几行的命令是`tail`
    +   `tac`按行逆序显示文件内容
    +   `rear`:没有此命令
    +   `last`:查看历史登录记录
+   以下不属于linux循环语句的是`select`
    +   `select`是SQL的查询语句

+   Shell程序中变量的引用方法是加`$`引用
+   正则表达式中表示行尾的定位元字符是`$`
    +   行首:`^`

+   根目录中存放系统配置文件的目录是`/etc`
    +   `/bin`:用户二进制文件
    +   `/dev`:设备文件
    +   `/proc`:进程信息
    +   `/var`:变量文件
    +   `/tmp`:临时文件
    +   `/usr`:用户程序
    +   `/home`:home目录
    +   `/boot`:引导加载文件
    +   `/lib`:系统库
    +   `/opt`:可选的附加应用程序

+   欲把当前目录下的file1.txt复制为file2.txt，正确的命令是`cp file1.txt file2.txt`
    +   对目录需要加`-r`参数,r表示recurtion

+   bash shell脚本的第一行是`#!/bin/bash`
+   文件名匹配时，通配符是指`?` `*` `[]`符号

---

**什么是链接？符号链接与硬链接的区别是什么？它们常用到什么地方？**

+   硬连接指通过索引节点来进行连接。在 Linux 的文件系统中，保存在磁盘分区中的文件不管是什么类型都给它分配一个编号，称为索引节点号(Inode Index)。在 Linux 中，多个文件名指向同一索引节点是存在的。
    硬链接相当于创建了文件的**同步更新**,是保存了文件属性的拷贝

+   另外一种连接称之为符号连接（Symbolic Link），也叫软连接。软链接文件有类似于 Windows 的**快捷方式**。它实际上是一个特殊的文件。在符号连接中，文件实际上是一个文本文件，其中包含的有另一文件的位置信息。

+   区别

    +   软链接在文件删除后不可访问,硬链接可以
    +   软连接可以跨分区使用,硬链接不行
    +   软连接可以对目录使用,硬链接不行

    1.硬链接原文件/链接文件公用一个inode号，说明他们是同一个文件，而软链接原文件/链接文件拥有不同的inode号，表明他们是两个不同的文件；
    2.在文件属性上软链接明确写出了是链接文件，而硬链接没有写出来，因为在本质上硬链接文件和原文件是完全平等关系；
    3.链接数目是不一样的，软链接的链接数目不会增加；
    4.文件大小是不一样的，硬链接文件显示的大小是跟原文件是一样的。

**vi编辑器有哪几种工作模式？它们有什么区别？如何在这几种工作模式之间转换**

+   命令模式(Command Mode)

    +       a ：在光标后开始插入
            
            A ：在行尾开始插入
            
            i ：从光标所在位置前面开始插入
            
            I ：从光标所在列的第一个非空白字元前面开始插入
            
            o ：在光标所在列下新增一列并进入输入模式
            
            O ：在光标所在列上方新增一列并进入输入模式
            
            ESC ：返回命令行模式

+   插入模式(Insert Mode)

    +   进行文本编辑

+   最终行模式(Last Line Mode)

    +   主要进行一些文字编辑辅助功能，比如字串搜索、替代、保存文件等操作。

>   在插入模式,最终行模式下,按下esc进入命令模式
>
>   在命令模式下,输入`:`进入最终行模式,输入`i/I`,`a/A`,`o/O`进入插入模式

